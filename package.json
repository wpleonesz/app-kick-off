{
  "name": "app-kick-off-ionic-login",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --host 0.0.0.0 --port 3001",
    "dev:debug": "DEBUG=* vite dev --host 0.0.0.0 --port 3001",
    "build": "vite build",
    "build:report": "vite build --outDir dist && du -sh dist",
    "preview": "vite preview --port 4173",
    "type-check": "tsc --noEmit",
    "lint:ts": "tsc --noEmit --pretty",
    "clean": "rm -rf dist node_modules package-lock.json && npm install",
    "clean:dist": "rm -rf dist",
    "clean:cache": "rm -rf node_modules/.vite && npm cache clean --force",
    "cap:init": "npx cap init app-kick-off com.example.appkickoff --web-dir=dist || true",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android",
    "cap:update": "npx cap update",
    "android": "npm run build && npx cap sync android && npx cap run android",
    "android:dev": "CAPACITOR_CONFIG=capacitor.config.dev.ts npx cap run android",
    "android:sync": "npm run build && npx cap sync android",
    "android:sync:only": "npx cap sync android",
    "android:build": "cd android && ./gradlew build && echo '✅ APK built: android/app/build/outputs/apk/'",
    "android:build:release": "cd android && ./gradlew bundleRelease && echo '✅ Bundle built: android/app/build/outputs/bundle/'",
    "android:build:clean": "cd android && ./gradlew clean build",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android",
    "android:logs": "adb logcat '*:S' ReactNative:V YOUR_APP_TAG:V",
    "android:debug": "adb shell input keyevent 82 && npm run android:logs",
    "ios": "npm run build && npx cap sync ios && npx cap run ios",
    "ios:dev": "CAPACITOR_CONFIG=capacitor.config.dev.ts npx cap run ios",
    "ios:sync": "npm run build && npx cap sync ios",
    "ios:sync:only": "npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:run": "npx cap run ios",
    "ios:logs": "log stream --predicate 'process == \"app-kick-off\"'",
    "ios:build": "cd ios && xcodebuild -workspace App.xcworkspace -scheme App -configuration Release",
    "web": "npm run dev",
    "web:build": "npm run build",
    "web:preview": "npm run build && npm run preview",
    "info": "npx cap doctor",
    "info:versions": "echo '=== Node ===' && node -v && echo '=== NPM ===' && npm -v && echo '=== Capacitor ===' && npx cap --version && echo '=== TypeScript ===' && npx tsc --version",
    "help": "echo '📱 App Kick Off - Available Commands' && echo '🚀 Development:' && echo '  npm run dev              - Start dev server (http://localhost:3001)' && echo '  npm run dev:debug        - Start with debug logs' && echo '🔨 Building:' && echo '  npm run build            - Build web bundle' && echo '  npm run build:report     - Build and show size' && echo '  npm run clean            - Clean everything' && echo '📱 Android:' && echo '  npm run android          - Full: build + sync + run' && echo '  npm run android:dev      - Dev mode with dev config' && echo '  npm run android:sync     - Build + sync only' && echo '  npm run android:build    - Build APK' && echo '  npm run android:build:release - Build release bundle' && echo '🍎 iOS:' && echo '  npm run ios              - Full: build + sync + run' && echo '  npm run ios:dev          - Dev mode with dev config' && echo '  npm run ios:sync         - Build + sync only' && echo '  npm run ios:build        - Build for iOS' && echo '🌐 Web:' && echo '  npm run web              - Start dev server' && echo '  npm run web:build        - Build for production' && echo '  npm run web:preview      - Build and preview' && echo '🛠️  Tools:' && echo '  npm run type-check       - Check TypeScript' && echo '  npm run info             - Show Capacitor info' && echo '  npm run info:versions    - Show versions'"
  },
  "dependencies": {
    "@capacitor-community/http": "^1.4.1",
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "@capawesome/capacitor-live-update": "^8.1.0",
    "@hookform/resolvers": "^5.2.2",
    "@ionic/pwa-elements": "^3.3.0",
    "@ionic/react": "^7.0.0",
    "@ionic/react-router": "^7.0.0",
    "@ionic/storage": "^4.0.0",
    "@tanstack/react-query": "^5.90.18",
    "@types/react-router-dom": "5",
    "history": "^5.3.0",
    "ionicons": "^8.0.13",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-hook-form": "^7.71.1",
    "react-router": "5",
    "react-router-dom": "5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
